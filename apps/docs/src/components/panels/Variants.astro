<div class="variants">
  <slot />
</div>

<style>
  .variants {
    margin-top: 36px;
    display: grid;
    grid-template-columns: 200px 1fr;
    border-top: 1px solid var(--color-line-2);
  }
  .variants :global(.variant-row) {
    display: contents;
  }

  @media (max-width: 768px) {
    .variants {
      margin: 18px var(--mobile-gutter, 20px) 0;
      display: block;
      grid-template-columns: 1fr;
    }
    .variants :global(.variant-row) {
      display: block;
    }
  }
</style>
