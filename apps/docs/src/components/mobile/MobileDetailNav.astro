<header class="mobile-detail-nav" data-mobile-detail-nav>
  <button class="back" type="button" data-mobile-back aria-label="Back to index">
    <svg
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      stroke-width="2"
      stroke-linecap="round"
      stroke-linejoin="round"
      aria-hidden="true"
    >
      <path d="M15 18l-6-6 6-6" />
    </svg>
    Index
  </button>
  <span class="counter" data-mobile-counter></span>
</header>

<style>
  .mobile-detail-nav {
    padding: max(56px, env(safe-area-inset-top, 0px) + 12px) var(--mobile-gutter-nav, 16px) 14px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: var(--color-surface);
    border-bottom: 1px solid var(--color-line-2);
    flex-shrink: 0;
    z-index: 2;
  }
  .back {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    background: none;
    border: none;
    padding: 6px 8px 6px 4px;
    color: var(--color-ink-2);
    font-size: 15px;
    font-weight: 500;
    cursor: pointer;
    font-family: inherit;
  }
  .back svg {
    width: 18px;
    height: 18px;
  }
  .counter {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--color-ink-5);
    font-variant-numeric: tabular-nums;
  }
</style>
