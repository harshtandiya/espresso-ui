---
title: Button
description: A flexible, accessible button component with multiple variants and sizes.
---

import Preview from "@/components/Preview.astro";
import { Button as ButtonReact } from "@/generated/react/Button.tsx";
import ButtonVue from "@/generated/vue/Button.vue";

# Button

A flexible, accessible button component with multiple variants and sizes.

## Installation

```sh
npm install -g @espresso-ui/cli
espresso-ui add button
```

## Usage

**React**

```tsx
import { Button } from "@/components/Button";

export default function App() {
  return <Button>Click me</Button>;
}
```

**Vue**

```vue
<script setup>
import Button from "@/components/Button.vue";
</script>

<template>
  <Button>Click me</Button>
</template>
```

## Variants

<Preview>
  <Fragment slot="react">
    <ButtonReact client:load variant="solid">
      Solid
    </ButtonReact>
    <ButtonReact client:load variant="outline">
      Outline
    </ButtonReact>
    <ButtonReact client:load variant="ghost">
      Ghost
    </ButtonReact>
    <ButtonReact client:load variant="link">
      Link
    </ButtonReact>
  </Fragment>
  <Fragment slot="vue">
    <ButtonVue client:load variant="solid">
      Solid
    </ButtonVue>
    <ButtonVue client:load variant="outline">
      Outline
    </ButtonVue>
    <ButtonVue client:load variant="ghost">
      Ghost
    </ButtonVue>
    <ButtonVue client:load variant="link">
      Link
    </ButtonVue>
  </Fragment>
</Preview>

## Sizes

<Preview>
  <Fragment slot="react">
    <ButtonReact client:load size="sm">
      Small
    </ButtonReact>
    <ButtonReact client:load size="md">
      Medium
    </ButtonReact>
    <ButtonReact client:load size="lg">
      Large
    </ButtonReact>
  </Fragment>
  <Fragment slot="vue">
    <ButtonVue client:load size="sm">
      Small
    </ButtonVue>
    <ButtonVue client:load size="md">
      Medium
    </ButtonVue>
    <ButtonVue client:load size="lg">
      Large
    </ButtonVue>
  </Fragment>
</Preview>

## Disabled

<Preview>
  <Fragment slot="react">
    <ButtonReact client:load disabled>
      Disabled
    </ButtonReact>
  </Fragment>
  <Fragment slot="vue">
    <ButtonVue client:load disabled>
      Disabled
    </ButtonVue>
  </Fragment>
</Preview>

## Props

| Prop       | Type                                        | Default   | Description                                    |
| ---------- | ------------------------------------------- | --------- | ---------------------------------------------- |
| `variant`  | `"solid" \| "outline" \| "ghost" \| "link"` | `"solid"` | Visual style of the button                     |
| `size`     | `"sm" \| "md" \| "lg"`                      | `"md"`    | Size of the button                             |
| `disabled` | `boolean`                                   | `false`   | Disables interaction and reduces opacity       |
| `asChild`  | `boolean`                                   | `false`   | Render as child element — React only           |
| `class`    | `string`                                    | —         | Additional classes merged via `tailwind-merge` |

## Theming

Button appearance is controlled entirely through CSS custom properties. Override any token in your own CSS to customise the look without touching the component source.

```css
:root {
  --btn-solid-bg: var(--color-primary);
  --btn-solid-fg: white;
  --btn-solid-bg-hover: color-mix(in oklch, var(--color-primary) 85%, black);
  --btn-radius: var(--radius-md);

  --btn-sm-height: 1.75rem;
  --btn-sm-px: 0.625rem;
  --btn-sm-font-size: 0.75rem;

  --btn-md-height: 2.25rem;
  --btn-md-px: 1rem;
  --btn-md-font-size: 0.875rem;

  --btn-lg-height: 2.75rem;
  --btn-lg-px: 1.25rem;
  --btn-lg-font-size: 1rem;
}
```
